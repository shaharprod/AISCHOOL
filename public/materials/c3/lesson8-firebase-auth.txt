מדריך Firebase Authentication
================================

שיעור 8: עבודה עם Firebase Auth
=================================

מבוא
====

בשיעור זה נלמד כיצד להטמיע
Firebase Authentication באפליקציה.

התקנה
======

```bash
npm install firebase
```

הגדרה
======

```javascript
// firebase.js
import { initializeApp } from 'firebase/app';
import { getAuth } from 'firebase/auth';

const firebaseConfig = {
  apiKey: "your-api-key",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project-id",
  // ...
};

const app = initializeApp(firebaseConfig);
export const auth = getAuth(app);
```

הרשמה
======

```jsx
import { createUserWithEmailAndPassword } from 'firebase/auth';
import { auth } from './firebase';

const signUp = async (email, password) => {
  try {
    const userCredential = await createUserWithEmailAndPassword(
      auth,
      email,
      password
    );
    console.log('User created:', userCredential.user);
  } catch (error) {
    console.error('Error:', error.message);
  }
};
```

התחברות
========

```jsx
import { signInWithEmailAndPassword } from 'firebase/auth';

const signIn = async (email, password) => {
  try {
    const userCredential = await signInWithEmailAndPassword(
      auth,
      email,
      password
    );
    console.log('User signed in:', userCredential.user);
  } catch (error) {
    console.error('Error:', error.message);
  }
};
```

התנתקות
========

```jsx
import { signOut } from 'firebase/auth';

const logout = async () => {
  try {
    await signOut(auth);
    console.log('User signed out');
  } catch (error) {
    console.error('Error:', error.message);
  }
};
```

מעקב אחר משתמש
===============

```jsx
import { onAuthStateChanged } from 'firebase/auth';
import { useEffect, useState } from 'react';

function App() {
  const [user, setUser] = useState(null);

  useEffect(() => {
    const unsubscribe = onAuthStateChanged(auth, (user) => {
      setUser(user);
    });

    return unsubscribe;
  }, []);

  return <div>{user ? `Welcome ${user.email}` : 'Please sign in'}</div>;
}
```

שיטות אימות נוספות
====================

Google Sign-In:
---------------

```jsx
import { GoogleAuthProvider, signInWithPopup } from 'firebase/auth';

const signInWithGoogle = async () => {
  const provider = new GoogleAuthProvider();
  try {
    const result = await signInWithPopup(auth, provider);
    console.log('User:', result.user);
  } catch (error) {
    console.error('Error:', error.message);
  }
};
```

סיכום
======

עכשיו אתה יודע:
✓ איך להתקין Firebase Auth
✓ איך להטמיע הרשמה והתחברות
✓ איך לעקוב אחר משתמש
✓ איך להשתמש ב-Google Sign-In

בשיעור הבא נלמד:
→ Firestore Database
→ עבודה עם Database

מוכנים להמשיך? 🚀

---
© 2026 Google AI Academy
מדריך Firebase Auth
גרסה 1.0
