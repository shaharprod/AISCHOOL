תרגילים מעשיים: הקמת פרויקט React
====================================

שיעור 4: תרגילים
================

מבוא
====

תרגילים מעשיים לתרגול הקמת פרויקט React.

תרגיל 1: פרויקט בסיסי
======================

מטרה: הקמת פרויקט React פשוט

צעדים:
1. צור פרויקט React חדש
2. שנה את הטקסט ב-App.jsx ל-"Hello World"
3. הוסף כפתור שמגדיל מונה
4. הרץ את הפרויקט

פתרון:

```jsx
// App.jsx
import { useState } from 'react';

function App() {
  const [count, setCount] = useState(0);

  return (
    <div>
      <h1>Hello World</h1>
      <button onClick={() => setCount(count + 1)}>
        Count: {count}
      </button>
    </div>
  );
}

export default App;
```

תרגיל 2: קומפוננטה מותאמת
===========================

מטרה: יצירת קומפוננטה Button מותאמת

צעדים:
1. צור תיקייה components/Button
2. צור קומפוננטה Button
3. הוסף props: text, onClick, variant
4. השתמש ב-Button ב-App

פתרון:

```jsx
// components/Button/Button.jsx
function Button({ text, onClick, variant = 'primary' }) {
  const className = `button button-${variant}`;
  
  return (
    <button className={className} onClick={onClick}>
      {text}
    </button>
  );
}

export default Button;

// App.jsx
import Button from './components/Button/Button';

function App() {
  return (
    <div>
      <Button 
        text="Click Me" 
        onClick={() => alert('Clicked!')}
        variant="primary"
      />
    </div>
  );
}
```

תרגיל 3: רשימת משימות
======================

מטרה: יצירת Todo List בסיסי

צעדים:
1. צור state ל-todos
2. הוסף input להוספת todo
3. הצג רשימת todos
4. הוסף אפשרות למחיקה

פתרון:

```jsx
import { useState } from 'react';

function App() {
  const [todos, setTodos] = useState([]);
  const [input, setInput] = useState('');

  const addTodo = () => {
    if (input.trim()) {
      setTodos([...todos, { id: Date.now(), text: input }]);
      setInput('');
    }
  };

  const deleteTodo = (id) => {
    setTodos(todos.filter(todo => todo.id !== id));
  };

  return (
    <div>
      <input 
        value={input}
        onChange={(e) => setInput(e.target.value)}
        onKeyPress={(e) => e.key === 'Enter' && addTodo()}
      />
      <button onClick={addTodo}>Add</button>
      
      <ul>
        {todos.map(todo => (
          <li key={todo.id}>
            {todo.text}
            <button onClick={() => deleteTodo(todo.id)}>Delete</button>
          </li>
        ))}
      </ul>
    </div>
  );
}
```

תרגיל 4: Custom Hook
=====================

מטרה: יצירת Hook מותאם

צעדים:
1. צור Hook useCounter
2. הוסף פונקציות: increment, decrement, reset
3. השתמש ב-Hook ב-App

פתרון:

```jsx
// hooks/useCounter.js
import { useState } from 'react';

function useCounter(initialValue = 0) {
  const [count, setCount] = useState(initialValue);

  const increment = () => setCount(count + 1);
  const decrement = () => setCount(count - 1);
  const reset = () => setCount(initialValue);

  return { count, increment, decrement, reset };
}

export default useCounter;

// App.jsx
import useCounter from './hooks/useCounter';

function App() {
  const { count, increment, decrement, reset } = useCounter(0);

  return (
    <div>
      <p>Count: {count}</p>
      <button onClick={increment}>+</button>
      <button onClick={decrement}>-</button>
      <button onClick={reset}>Reset</button>
    </div>
  );
}
```

תרגיל 5: Form עם Validation
=============================

מטרה: יצירת Form עם בדיקות

צעדים:
1. צור Form ל-Registration
2. הוסף fields: name, email, password
3. הוסף validation
4. הצג שגיאות

פתרון:

```jsx
import { useState } from 'react';

function App() {
  const [formData, setFormData] = useState({
    name: '',
    email: '',
    password: ''
  });
  const [errors, setErrors] = useState({});

  const validate = () => {
    const newErrors = {};
    
    if (!formData.name) {
      newErrors.name = 'Name is required';
    }
    
    if (!formData.email) {
      newErrors.email = 'Email is required';
    } else if (!/\S+@\S+\.\S+/.test(formData.email)) {
      newErrors.email = 'Email is invalid';
    }
    
    if (!formData.password) {
      newErrors.password = 'Password is required';
    } else if (formData.password.length < 6) {
      newErrors.password = 'Password must be at least 6 characters';
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (validate()) {
      console.log('Form submitted:', formData);
    }
  };

  return (
    <form onSubmit={handleSubmit}>
      <div>
        <input
          value={formData.name}
          onChange={(e) => setFormData({...formData, name: e.target.value})}
          placeholder="Name"
        />
        {errors.name && <span>{errors.name}</span>}
      </div>
      
      <div>
        <input
          type="email"
          value={formData.email}
          onChange={(e) => setFormData({...formData, email: e.target.value})}
          placeholder="Email"
        />
        {errors.email && <span>{errors.email}</span>}
      </div>
      
      <div>
        <input
          type="password"
          value={formData.password}
          onChange={(e) => setFormData({...formData, password: e.target.value})}
          placeholder="Password"
        />
        {errors.password && <span>{errors.password}</span>}
      </div>
      
      <button type="submit">Submit</button>
    </form>
  );
}
```

תרגיל 6: API Call
==================

מטרה: קריאה ל-API

צעדים:
1. השתמש ב-fetch
2. קרא מ-API חיצוני
3. הצג נתונים
4. הוסף loading state

פתרון:

```jsx
import { useState, useEffect } from 'react';

function App() {
  const [data, setData] = useState(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);

  useEffect(() => {
    fetch('https://jsonplaceholder.typicode.com/users')
      .then(res => res.json())
      .then(data => {
        setData(data);
        setLoading(false);
      })
      .catch(err => {
        setError(err.message);
        setLoading(false);
      });
  }, []);

  if (loading) return <div>Loading...</div>;
  if (error) return <div>Error: {error}</div>;

  return (
    <div>
      <h1>Users</h1>
      <ul>
        {data?.map(user => (
          <li key={user.id}>{user.name}</li>
        ))}
      </ul>
    </div>
  );
}
```

תרגיל 7: Router בסיסי
======================

מטרה: הוספת Routing

צעדים:
1. התקן react-router-dom
2. צור 3 דפים: Home, About, Contact
3. הוסף Navigation
4. הגדר Routes

פתרון:

```jsx
// App.jsx
import { BrowserRouter, Routes, Route, Link } from 'react-router-dom';
import Home from './pages/Home';
import About from './pages/About';
import Contact from './pages/Contact';

function App() {
  return (
    <BrowserRouter>
      <nav>
        <Link to="/">Home</Link>
        <Link to="/about">About</Link>
        <Link to="/contact">Contact</Link>
      </nav>
      
      <Routes>
        <Route path="/" element={<Home />} />
        <Route path="/about" element={<About />} />
        <Route path="/contact" element={<Contact />} />
      </Routes>
    </BrowserRouter>
  );
}
```

סיכום
======

תרגלת:
✓ פרויקט בסיסי
✓ קומפוננטות
✓ State Management
✓ Forms
✓ API Calls
✓ Routing

המשך לתרגל:
• צור פרויקט משלך
• נסה features חדשים
• תתרגל באופן קבוע

בשיעור הבא נלמד:
→ בניית API
→ עבודה עם Backend

מוכנים להמשיך? 🚀

---
© 2026 Google AI Academy
תרגילים מעשיים
גרסה 1.0
