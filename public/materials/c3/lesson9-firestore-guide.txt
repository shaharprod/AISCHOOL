מדריך Firestore Database
==========================

שיעור 9: Firestore Database
============================

מבוא
====

בשיעור זה נלמד כיצד לעבוד עם
Firestore Database ב-Firebase.

התקנה
======

```bash
npm install firebase
```

הגדרה
======

```javascript
// firebase.js
import { initializeApp } from 'firebase/app';
import { getFirestore } from 'firebase/firestore';

const app = initializeApp(firebaseConfig);
export const db = getFirestore(app);
```

יצירת נתונים
=============

```jsx
import { collection, addDoc } from 'firebase/firestore';
import { db } from './firebase';

const addUser = async () => {
  try {
    const docRef = await addDoc(collection(db, 'users'), {
      name: 'John',
      email: 'john@example.com'
    });
    console.log('Document written with ID:', docRef.id);
  } catch (error) {
    console.error('Error:', error);
  }
};
```

קריאת נתונים
=============

```jsx
import { collection, getDocs } from 'firebase/firestore';

const getUsers = async () => {
  const querySnapshot = await getDocs(collection(db, 'users'));
  querySnapshot.forEach((doc) => {
    console.log(doc.id, '=>', doc.data());
  });
};
```

עדכון נתונים
=============

```jsx
import { doc, updateDoc } from 'firebase/firestore';

const updateUser = async (userId) => {
  const userRef = doc(db, 'users', userId);
  await updateDoc(userRef, {
    name: 'Jane'
  });
};
```

מחיקת נתונים
=============

```jsx
import { doc, deleteDoc } from 'firebase/firestore';

const deleteUser = async (userId) => {
  await deleteDoc(doc(db, 'users', userId));
};
```

Real-time Updates
=================

```jsx
import { collection, onSnapshot } from 'firebase/firestore';

useEffect(() => {
  const unsubscribe = onSnapshot(
    collection(db, 'users'),
    (snapshot) => {
      const users = snapshot.docs.map(doc => ({
        id: doc.id,
        ...doc.data()
      }));
      setUsers(users);
    }
  );

  return unsubscribe;
}, []);
```

סיכום
======

עכשיו אתה יודע:
✓ איך לעבוד עם Firestore
✓ איך ליצור, לקרוא, לעדכן ולמחוק נתונים
✓ איך להשתמש ב-Real-time Updates

בשיעור הבא נלמד:
→ Testing ב-IDX
→ כתיבת בדיקות

מוכנים להמשיך? 🚀

---
© 2026 Google AI Academy
מדריך Firestore
גרסה 1.0
