מדריך פריסה ל-Firebase Hosting
=================================

שיעור 6: פריסה ל-Firebase
==========================

מבוא
====

בשיעור זה נלמד כיצד לפרוס אפליקציה
ל-Firebase Hosting מ-Project IDX.

הכנה
=====

1. התקן Firebase CLI:
---------------------

```bash
npm install -g firebase-tools
```

2. התחבר ל-Firebase:
---------------------

```bash
firebase login
```

3. אתחל פרויקט:
----------------

```bash
firebase init
```

בחר:
• Hosting
• Use existing project (או צור חדש)
• Public directory: dist (או build)
• Single-page app: Yes
• Set up automatic builds: No

קבצי הגדרה
===========

firebase.json:
--------------

```json
{
  "hosting": {
    "public": "dist",
    "ignore": [
      "firebase.json",
      "**/.*",
      "**/node_modules/**"
    ],
    "rewrites": [
      {
        "source": "**",
        "destination": "/index.html"
      }
    ]
  }
}
```

.firebaserc:
------------

```json
{
  "projects": {
    "default": "your-project-id"
  }
}
```

פריסה
======

1. Build הפרויקט:
-----------------

```bash
npm run build
```

2. פרוס ל-Firebase:
--------------------

```bash
firebase deploy
```

3. או רק Hosting:
-----------------

```bash
firebase deploy --only hosting
```

עדכון
======

לעדכן פריסה קיימת:

```bash
npm run build
firebase deploy
```

פתרון בעיות
============

1. "Firebase CLI not found"
   → התקן: npm install -g firebase-tools

2. "Permission denied"
   → הרץ: firebase login

3. "Build failed"
   → בדוק שגיאות build
   → ודא שכל התלויות מותקנות

סיכום
======

עכשיו אתה יודע:
✓ איך להתקין Firebase CLI
✓ איך לאתחל פרויקט
✓ איך לפרוס אפליקציה

בשיעור הבא נלמד:
→ טיפול בשגיאות
→ Error Handling

מוכנים להמשיך? 🚀

---
© 2026 Google AI Academy
מדריך פריסה ל-Firebase
גרסה 1.0
