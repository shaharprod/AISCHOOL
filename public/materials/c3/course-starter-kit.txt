Project IDX Starter Kit
========================

מדריך הקמת פרויקט מלא
=======================

🚀 מבוא
========

ברוכים הבאים ל-Project IDX Starter Kit!
מדריך זה יעזור לך להקים פרויקט חדש בצורה מקצועית.

מה זה Project IDX?
-------------------
• סביבת פיתוח בענן מבית Google
• מבוססת על VS Code
• Gemini Code Assist מובנה
• תבניות מוכנות
• סימולטורים מובנים
• חינמית!

גישה:
📍 idx.google.com

התחלה מהירה
=============

שלב 1: התחברות
---------------

1. גש ל-idx.google.com
2. התחבר עם חשבון Google
3. לחץ "Create" או "New Project"

שלב 2: בחירת תבנית
-------------------

תבניות זמינות:

Frontend:
• React + Vite
• Next.js
• Angular
• Vue.js
• Svelte
• HTML/CSS/JS

Backend:
• Node.js + Express
• Python + Flask
• Python + Django
• Go
• Ruby on Rails

Fullstack:
• MERN Stack
• T3 Stack
• Firebase
• Remix

Mobile:
• Flutter
• React Native
• Angular + Capacitor

AI/ML:
• Python + TensorFlow
• Python + PyTorch
• Gemini API Integration

ריק:
• Start from scratch

המלצה למתחילים:
→ React + Vite (קל וחזק)

שלב 3: הגדרת הפרויקט
---------------------

שדות:
• שם הפרויקט
• תיאור (אופציונלי)
• Public/Private
• Git Repository (מתחבר אוטומטית)

טיפים:
💡 שם ברור ותיאורי
💡 Private למיזמים אישיים
💡 Public לקוד פתוח

מבנה פרויקט מומלץ
==================

פרויקט React בסיסי
-------------------

```
my-project/
├── src/
│   ├── components/
│   │   ├── Header.jsx
│   │   ├── Footer.jsx
│   │   └── ...
│   ├── pages/
│   │   ├── Home.jsx
│   │   ├── About.jsx
│   │   └── ...
│   ├── styles/
│   │   ├── App.css
│   │   └── ...
│   ├── utils/
│   │   └── helpers.js
│   ├── App.jsx
│   └── main.jsx
├── public/
│   └── assets/
├── .env
├── .gitignore
├── package.json
├── vite.config.js
└── README.md
```

פרויקט Fullstack
-----------------

```
my-fullstack-app/
├── client/              # Frontend
│   ├── src/
│   ├── public/
│   └── package.json
├── server/              # Backend
│   ├── routes/
│   ├── models/
│   ├── controllers/
│   ├── middleware/
│   ├── config/
│   ├── server.js
│   └── package.json
├── shared/              # קוד משותף
│   └── types/
├── .env
├── .gitignore
└── README.md
```

קבצים חשובים
=============

1. package.json
---------------

דוגמה:
```json
{
  "name": "my-project",
  "version": "1.0.0",
  "description": "My awesome project",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^5.0.0"
  }
}
```

2. .env
-------

דוגמה:
```
# API Keys (אל תשתף!)
VITE_API_KEY=your_api_key_here
VITE_GEMINI_API_KEY=your_gemini_key

# URLs
VITE_API_URL=http://localhost:3000
VITE_APP_URL=http://localhost:5173

# Environment
NODE_ENV=development
```

⚠️ תמיד הוסף ל-.gitignore!

3. .gitignore
-------------

דוגמה:
```
# Dependencies
node_modules/
.pnp
.pnp.js

# Testing
coverage/

# Production
build/
dist/

# Environment
.env
.env.local
.env.development
.env.production

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
```

4. README.md
------------

תבנית:
```markdown
# שם הפרויקט

תיאור קצר של הפרויקט

## תכונות

- תכונה 1
- תכונה 2
- תכונה 3

## התקנה

\`\`\`bash
npm install
\`\`\`

## הרצה

\`\`\`bash
npm run dev
\`\`\`

## בנייה

\`\`\`bash
npm run build
\`\`\`

## טכנולוגיות

- React 18
- Vite 5
- Tailwind CSS

## רישיון

MIT
```

התקנת חבילות
=============

חבילות בסיסיות
---------------

React + Routing:
```bash
npm install react-router-dom
```

Styling:
```bash
npm install tailwindcss @tailwindcss/vite
```

State Management:
```bash
npm install zustand
# או
npm install @tanstack/react-query
```

Forms:
```bash
npm install react-hook-form zod
```

UI Components:
```bash
npm install shadcn-ui
# או
npm install @mui/material @emotion/react
```

Icons:
```bash
npm install lucide-react
# או
npm install react-icons
```

HTTP Client:
```bash
npm install axios
```

Date/Time:
```bash
npm install date-fns
# או
npm install dayjs
```

חבילות מתקדמות
----------------

AI Integration:
```bash
npm install @google/generative-ai
```

Authentication:
```bash
npm install firebase
# או
npm install @supabase/supabase-js
```

Testing:
```bash
npm install -D vitest @testing-library/react
```

Linting:
```bash
npm install -D eslint @typescript-eslint/parser
```

הגדרות מומלצות
================

1. TypeScript (מומלץ מאוד!)
----------------------------

התקנה:
```bash
npm install -D typescript @types/react @types/react-dom
```

tsconfig.json:
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

2. ESLint
---------

.eslintrc.json:
```json
{
  "extends": [
    "eslint:recommended",
    "plugin:react/recommended",
    "plugin:react-hooks/recommended"
  ],
  "rules": {
    "react/react-in-jsx-scope": "off",
    "react/prop-types": "off"
  }
}
```

3. Prettier
-----------

.prettierrc:
```json
{
  "semi": true,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "es5",
  "printWidth": 80
}
```

4. Tailwind CSS
---------------

tailwind.config.js:
```js
/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
```

Gemini Code Assist
==================

הפעלה:
------

1. לחץ Ctrl+I (או Cmd+I במק)
2. כתוב מה אתה רוצה
3. Gemini יעזור לך!

דוגמאות שימוש:
---------------

1. יצירת קומפוננטה:
```
Create a React component called Button with props: 
text, onClick, variant (primary/secondary)
```

2. הוספת פונקציונליות:
```
Add form validation to this form using react-hook-form
```

3. תיקון באגים:
```
Why is this useState not updating? [הדבק קוד]
```

4. שיפור קוד:
```
Improve this code for better performance and readability
```

5. הסבר:
```
Explain what this code does line by line
```

Preview ובדיקה
===============

הרצת פרויקט
------------

Terminal:
```bash
npm run dev
```

Preview:
• IDX פותח preview אוטומטית
• או לחץ "Open in Browser"
• URL: https://[project-id].idx.app

Hot Reload:
• שינויים מתעדכנים אוטומטית
• לא צריך לרענן

סימולטורים
-----------

Mobile:
• לחץ על אייקון טלפון
• בחר מכשיר (iPhone/Android)
• בדוק responsive

Tablet:
• בחר מכשיר טאבלט
• iPad, Galaxy Tab, וכו'

Desktop:
• רזולוציות שונות
• 1920x1080, 1366x768, וכו'

Git ו-Version Control
======================

התחלת Git
----------

IDX יוצר repo אוטומטית!

פקודות בסיסיות:
```bash
git status
git add .
git commit -m "Initial commit"
git push
```

Branches:
```bash
git checkout -b feature/new-feature
git add .
git commit -m "Add new feature"
git push origin feature/new-feature
```

Gemini עוזר:
```
Help me write a good commit message for these changes:
[הסבר מה שינית]
```

Deploy ופריסה
==============

אופציות:
---------

1. Firebase Hosting (מומלץ!)
   • חינמי
   • מהיר
   • אינטגרציה מובנית

2. Netlify
   • חינמי
   • CI/CD אוטומטי
   • DNS management

3. Vercel
   • חינמי
   • מושלם ל-Next.js
   • Edge Functions

4. GitHub Pages
   • חינמי
   • פשוט
   • ל-static sites

Deploy ל-Firebase:
-------------------

```bash
# התקנה
npm install -g firebase-tools

# התחברות
firebase login

# אתחול
firebase init hosting

# בנייה
npm run build

# פריסה
firebase deploy
```

IDX עושה זאת אוטומטית!
→ לחץ Deploy → בחר Firebase

בעיות נפוצות
=============

1. "Module not found"
---------------------

פתרון:
```bash
npm install
# או
npm install [package-name]
```

2. Port כבר בשימוש
-------------------

פתרון:
• סגור טרמינל אחר
• או שנה port ב-vite.config.js

3. שינויים לא מתעדכנים
-----------------------

פתרון:
• Ctrl+Shift+R (hard refresh)
• נקה cache
• הפעל מחדש dev server

4. Gemini לא עובד
------------------

פתרון:
• בדוק חיבור לאינטרנט
• רענן דף
• התחבר מחדש

5. Build נכשל
--------------

פתרון:
• בדוק שגיאות ב-console
• תקן warnings
• בדוק dependencies

טיפים מקצועיים
================

1. ארגון
--------
💡 תיקיות ברורות
💡 שמות קבצים עקביים
💡 רכיבים קטנים וממוקדים

2. ביצועים
-----------
💡 Lazy loading לדפים
💡 Image optimization
💡 Code splitting

3. אבטחה
---------
💡 .env לסודות
💡 אימות input
💡 HTTPS בייצור

4. בדיקות
----------
💡 כתוב tests
💡 בדוק Edge cases
💡ּ CI/CD

5. תיעוד
---------
💡 README מעודכן
💡 הערות בקוד
💡 דוגמאות שימוש

צ'ק ליסט הקמת פרויקט
======================

□ נוצר פרויקט חדש ב-IDX
□ בחרתי תבנית מתאימה
□ הגדרתי package.json
□ יצרתי .env ו-.gitignore
□ התקנתי חבילות בסיסיות
□ הגדרתי ESLint/Prettier
□ יצרתי מבנה תיקיות
□ כתבתי README
□ בדקתי preview
□ עשיתי commit ראשון
□ הגדרתי deployment

סיכום
=====

✓ הקמת פרויקט מהירה
✓ מבנה מאורגן
✓ Gemini מסייע
✓ Preview בזמן אמת
✓ Git מובנה
✓ Deploy קל

התחל עכשיו:
1. צור פרויקט חדש
2. בחר תבנית
3. התקן חבילות
4. התחל לקוד! 💻

---
© 2026 Google AI Academy
IDX Starter Kit
גרסה 2.0
