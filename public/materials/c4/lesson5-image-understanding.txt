Image Understanding - המדריך השלם
====================================

שיעור 5: Image Understanding
==============================

מה זה Image Understanding?
===========================

Image Understanding היא היכולת של המודל לנתח ולהבין תמונות:
• זיהוי אובייקטים
• תיאור תמונות
• שאלות על תמונות
• ניתוח ויזואלי

למה זה חשוב?
=============

• עבודה עם תמונות
• ניתוח ויזואלי
• זיהוי תוכן
• יצירת תוכן מבוסס תמונות

איך זה עובד?
=============

1. העלאת תמונה
---------------

Python:
```python
import google.generativeai as genai
import PIL.Image

genai.configure(api_key="YOUR_API_KEY")

# טעינת תמונה
img = PIL.Image.open("image.jpg")

model = genai.GenerativeModel("gemini-1.5-pro-vision")
response = model.generate_content(["תאר את התמונה", img])
print(response.text)
```

JavaScript:
```javascript
const { GoogleGenerativeAI } = require("@google/generative-ai");
const fs = require("fs");

const genAI = new GoogleGenerativeAI("YOUR_API_KEY");
const model = genAI.getGenerativeModel({ model: "gemini-1.5-pro-vision" });

// קריאת תמונה
const imageData = fs.readFileSync("image.jpg");
const imagePart = {
    inlineData: {
        data: imageData.toString("base64"),
        mimeType: "image/jpeg"
    }
};

const result = await model.generateContent([
    "תאר את התמונה",
    imagePart
]);
console.log(result.response.text());
```

2. תיאור תמונה בסיסי
---------------------

```python
def describe_image(image_path, language="hebrew"):
    img = PIL.Image.open(image_path)
    model = genai.GenerativeModel("gemini-1.5-pro-vision")
    
    prompt = f"תאר את התמונה בפירוט בעברית"
    response = model.generate_content([prompt, img])
    return response.text

# שימוש
description = describe_image("photo.jpg")
print(description)
```

3. שאלות על תמונה
-------------------

```python
def ask_about_image(image_path, question):
    img = PIL.Image.open(image_path)
    model = genai.GenerativeModel("gemini-1.5-pro-vision")
    
    response = model.generate_content([question, img])
    return response.text

# שימוש
answer = ask_about_image("photo.jpg", "מה הצבעים העיקריים בתמונה?")
print(answer)
```

דוגמאות שימושיות
==================

דוגמה 1: זיהוי אובייקטים
---------------------------

```python
def identify_objects(image_path):
    img = PIL.Image.open(image_path)
    model = genai.GenerativeModel("gemini-1.5-pro-vision")
    
    prompt = """זהה את כל האובייקטים בתמונה.
החזר רשימה של כל האובייקטים עם מיקומם."""
    
    response = model.generate_content([prompt, img])
    return response.text

# שימוש
objects = identify_objects("photo.jpg")
print(objects)
```

דוגמה 2: ניתוח סנטימנט ויזואלי
---------------------------------

```python
def analyze_visual_sentiment(image_path):
    img = PIL.Image.open(image_path)
    model = genai.GenerativeModel("gemini-1.5-pro-vision")
    
    prompt = """נתח את הסנטימנט של התמונה.
מה הרגש שהתמונה מעבירה?
האם היא חיובית, שלילית או ניטרלית?"""
    
    response = model.generate_content([prompt, img])
    return response.text

# שימוש
sentiment = analyze_visual_sentiment("photo.jpg")
print(sentiment)
```

דוגמה 3: ניתוח טקסט בתמונה (OCR)
-----------------------------------

```python
def extract_text_from_image(image_path):
    img = PIL.Image.open(image_path)
    model = genai.GenerativeModel("gemini-1.5-pro-vision")
    
    prompt = """קרא את כל הטקסט בתמונה.
החזר את הטקסט בפורמט נקי ומאורגן."""
    
    response = model.generate_content([prompt, img])
    return response.text

# שימוש
text = extract_text_from_image("document.jpg")
print(text)
```

דוגמה 4: השוואת תמונות
-------------------------

```python
def compare_images(image1_path, image2_path):
    img1 = PIL.Image.open(image1_path)
    img2 = PIL.Image.open(image2_path)
    model = genai.GenerativeModel("gemini-1.5-pro-vision")
    
    prompt = """השווה בין שתי התמונות.
מה הדמיון והשוני ביניהן?"""
    
    response = model.generate_content([prompt, img1, img2])
    return response.text

# שימוש
comparison = compare_images("photo1.jpg", "photo2.jpg")
print(comparison)
```

דוגמה 5: ניתוח מוצר בתמונה
-----------------------------

```python
def analyze_product(image_path):
    img = PIL.Image.open(image_path)
    model = genai.GenerativeModel("gemini-1.5-pro-vision")
    
    prompt = """נתח את המוצר בתמונה.
כלול:
- שם המוצר
- תכונות עיקריות
- איכות נראית
- הערכה כללית"""
    
    response = model.generate_content([prompt, img])
    return response.text

# שימוש
analysis = analyze_product("product.jpg")
print(analysis)
```

דוגמה 6: זיהוי אנשים ופעילויות
----------------------------------

```python
def identify_people_and_activities(image_path):
    img = PIL.Image.open(image_path)
    model = genai.GenerativeModel("gemini-1.5-pro-vision")
    
    prompt = """זהה אנשים ופעילויות בתמונה.
תאר מה כל אדם עושה."""
    
    response = model.generate_content([prompt, img])
    return response.text

# שימוש
description = identify_people_and_activities("event.jpg")
print(description)
```

דוגמה 7: ניתוח נוף
--------------------

```python
def analyze_landscape(image_path):
    img = PIL.Image.open(image_path)
    model = genai.GenerativeModel("gemini-1.5-pro-vision")
    
    prompt = """נתח את הנוף בתמונה.
כלול:
- סוג נוף
- אלמנטים עיקריים
- תנאי מזג אוויר
- שעה ביום (אם ניתן לזהות)"""
    
    response = model.generate_content([prompt, img])
    return response.text

# שימוש
landscape = analyze_landscape("landscape.jpg")
print(landscape)
```

דוגמה 8: יצירת תיאור שיווקי
-------------------------------

```python
def create_marketing_description(image_path):
    img = PIL.Image.open(image_path)
    model = genai.GenerativeModel("gemini-1.5-pro-vision")
    
    prompt = """צור תיאור שיווקי מקצועי לתמונה.
התיאור צריך להיות:
- משכנע
- ממוקד ב-benefits
- מתאים לקהל רחב"""
    
    response = model.generate_content([prompt, img])
    return response.text

# שימוש
marketing_text = create_marketing_description("product.jpg")
print(marketing_text)
```

דוגמה 9: זיהוי בעיות/נזקים
-----------------------------

```python
def identify_issues(image_path):
    img = PIL.Image.open(image_path)
    model = genai.GenerativeModel("gemini-1.5-pro-vision")
    
    prompt = """זהה בעיות או נזקים בתמונה.
תאר מה הבעיה ואיפה היא נמצאת."""
    
    response = model.generate_content([prompt, img])
    return response.text

# שימוש
issues = identify_issues("damage.jpg")
print(issues)
```

דוגמה 10: ניתוח ארכיטקטורה
-----------------------------

```python
def analyze_architecture(image_path):
    img = PIL.Image.open(image_path)
    model = genai.GenerativeModel("gemini-1.5-pro-vision")
    
    prompt = """נתח את הארכיטקטורה בתמונה.
כלול:
- סגנון ארכיטקטוני
- אלמנטים עיצוביים
- תקופה משוערת
- מאפיינים מיוחדים"""
    
    response = model.generate_content([prompt, img])
    return response.text

# שימוש
architecture = analyze_architecture("building.jpg")
print(architecture)
```

עבודה עם תמונות מרובות
========================

```python
def analyze_multiple_images(image_paths, question):
    images = [PIL.Image.open(path) for path in image_paths]
    model = genai.GenerativeModel("gemini-1.5-pro-vision")
    
    content = [question] + images
    response = model.generate_content(content)
    return response.text

# שימוש
images = ["img1.jpg", "img2.jpg", "img3.jpg"]
analysis = analyze_multiple_images(
    images,
    "מה המשותף לכל התמונות?"
)
print(analysis)
```

שילוב עם JSON Mode
===================

```python
def analyze_image_structured(image_path):
    img = PIL.Image.open(image_path)
    model = genai.GenerativeModel(
        "gemini-1.5-pro-vision",
        generation_config={
            "response_mime_type": "application/json",
            "response_schema": {
                "type": "object",
                "properties": {
                    "description": {"type": "string"},
                    "objects": {
                        "type": "array",
                        "items": {"type": "string"}
                    },
                    "colors": {
                        "type": "array",
                        "items": {"type": "string"}
                    },
                    "sentiment": {"type": "string"}
                },
                "required": ["description", "objects"]
            }
        }
    )
    
    response = model.generate_content([
        "נתח את התמונה בפירוט",
        img
    ])
    
    return json.loads(response.text)

# שימוש
result = analyze_image_structured("photo.jpg")
print(result["description"])
print(result["objects"])
```

Best Practices
==============

1. איכות תמונה
---------------
• השתמש בתמונות באיכות טובה
• ודא שהתמונה ברורה
• הימנע מתמונות מטושטשות

2. פורמטים נתמכים
-------------------
• JPEG
• PNG
• WebP
• GIF (frame ראשון)

3. גודל תמונה
--------------
• מקסימום: 20MB
• מומלץ: פחות מ-5MB
• רזולוציה: עד 3072x3072

4. שאלות ברורות
-----------------
• שאל שאלות ספציפיות
• הוסף הקשר כשצריך
• השתמש בשפה ברורה

5. טיפול בשגיאות
-----------------
```python
def safe_image_analysis(image_path, prompt):
    try:
        img = PIL.Image.open(image_path)
        model = genai.GenerativeModel("gemini-1.5-pro-vision")
        response = model.generate_content([prompt, img])
        return response.text
    except FileNotFoundError:
        return "קובץ תמונה לא נמצא"
    except Exception as e:
        return f"שגיאה: {e}"

# שימוש
result = safe_image_analysis("photo.jpg", "תאר את התמונה")
```

סיכום
======

Image Understanding מאפשר:
✓ זיהוי אובייקטים
✓ תיאור תמונות
✓ שאלות על תמונות
✓ ניתוח ויזואלי
✓ עבודה עם תמונות מרובות
✓ שילוב עם JSON Mode

Best Practices:
• איכות תמונה טובה
• פורמטים נתמכים
• גודל מתאים
• שאלות ברורות
• טיפול בשגיאות

בשיעור הבא: Safety Settings! 🚀

---
© 2026 Google AI Academy
Image Understanding - המדריך השלם